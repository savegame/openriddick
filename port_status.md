# Статус порта OpenRiddick (Linux/SDL2/GLES3)

Единый файл состояния работ по порту: что делается сейчас, что уже сделано,
что в очереди. Обновлять при каждой смене этапа. Подробные разборы — в
`Docs/` (спеки и отчёты), карта хаков и env-флагов — в `Docs/HacksAndHooks.md`
и `AGENTS.md`.

Ветка: `riddick-render`. Дата среза: 2026-08-03.

---

## Сейчас в работе

### A. Скриптовая система и анимации персонажей — ГЛАВНОЕ

Симптомы, наблюдаемые глазами:

* **невозможно начать диалог ни с одним NPC** (самая заметная бага);
* часть костей персонажа не анимируется (плечи стоят, предплечья двигаются;
  вторая рука в модельной позе; NPC делает первый кадр шага и скользит);
* в катсценах анимация нормальная;
* в скриптовых сценах некоторые персонажи не на своих местах и не идут к
  целям.

Что уже установлено измерением (полностью — `Docs/Research_BoneAnimation.md`):

* **скелет и скиннинг здоровы**, кости просто не получают анимации:
  у замороженных костей 15/20/27/28 трек существует и маска его включает, но
  значение трека — единичный кватернион, то есть ни один слой AG2 его не
  двигает; кости 22/23 (зеркальная пара, ключицы/плечи) держат осмысленный,
  но неизменный поворот;
* закрыты версии: VB-арена, индексы, палитра >64 костей, GPU-скиннинг,
  «хвост скелета — наш баг загрузки» (сверено с ретейлом по декомпайлу).

**Загрузка диалогов — закрыто, дефекта нет** (прогон `pa2_diner`,
2026-08-03). Имя диалога уже несёт подпуть (`PA2/Pa2_Diner/Dlg_Georgie`),
плоский путь `DIALOGUES\<Name>.XRG` работает, регистр и слэши снимает
`Linux_ResolvePath`. Из 30 запросов не нашлись 3, и все 3 отсутствуют в
PC-наборе. Контейнер `All.xcd` — PS3-упаковка, его отсутствие норма.

**Звук как корень — версия ОТМЕНЕНА измерением** (второй прогон,
`Docs/Research_Scripts_Dialogue.md` §12):

* парсер `.xsfxc` в порядке: 6724 DESC-узла, **0** без `*Source`, не
  собралось 167 (2.5 %); 392 «неизвестных узла» оказались все `*CATEGORY`,
  а это не дескриптор;
* 177 `Undefined sound` — чужой контент: `Gam_drk_*`, `D_Hurts_Darkl/Jackie`,
  `tentacle`, `CreepingDark` — это **The Darkness**, чей игровой код лежит
  в этом же снапшоте `GameClasses`; плюс UI и мультиплеерные звуки;
* нулевых длин реплики за прогон — **три**, и ни одна не из-за
  отсутствующего дескриптора (`[DLGLEN] no dialogue item (hash miss)`).

То есть цепочка «нет дескриптора → длина 0 → NPC молчит» в реальном
прогоне не срабатывает. Приоритет «сначала звук» снят.

**«Нельзя начать диалог ни с одним NPC» — не тот путь, что мы смотрели.**
`failed Dialogue` — это AI, решивший заговорить сам. Диалог, инициируемый
ИГРОКОМ, идёт через фокус/Use → запуск диалога, и его мы не смотрели ещё
ни разу. Разбирать надо там.

**Анимграф здоров целиком.** Штатная трасса движка (557 входов в
состояние): состояния сменяются, включая `EXPLORE_WALKFWD`, `RUNFWD`,
`TURNRIGHT45`, `BEHAVIOR_TABLESITTER_EAT`, `DIALOG_FACIAL_MOOD_*`; у
каждого резолвится анимация (`iAnim 2/74/667/1027`); игровое время идёт.
Версия «переходы не выполняются» закрыта.

**Осталась одна рабочая версия — застрявшее время слоя.** Поза берётся в
`Layer.m_Time` (= `LoopedTime`), а корневое движение — как разность
`EvalTrack0(m_Time)` и `EvalTrack0(m_Time + TimeSpan·TimeScale)`. Если
`LoopedTime` не растёт, разность каждый тик одна и та же и ненулевая:
персонаж едет с постоянной скоростью, а поза стоит на первом кадре — ровно
то, что на экране. Это же объясняет старый замер, где скорость от анимации
вышла в 3-6 раз больше расчётной. Отдельная версия внутри той же: рисуется
**клиентский** инстанс AG2, а трасса собиралась **серверная**.

**Навигация:** `[NAV] navgrid: cells 320x384x256` здоров,
`navgraph: nodes=17 edges=37 sizes=0x1` — подозрительно мало, и для крупных
персонажей графа нет вовсе. Наблюдение, не диагноз; фоном.

Отдельно: «звук есть, рот не открывается» — ожидаемо, FaceFX-обёртки
исключены из сборки (`CLAUDE.md`, Фаза 2).

Следующий шаг — один зонд, `RIDDICK_DBG_ANIMTIME=1`:

```
[ANIMT] srv|cli obj=N iAnim=N gt= enter= contin= looped= scale= dur= blend=
```

`looped` растёт → время идёт, дефект ниже (в `CXR_Skeleton::EvalAnim` /
выборке треков); `looped` стоит → нашли; строк `cli` нет вовсе →
клиентский анимграф не работает, и это ответ.

Дальше в очереди: диалог по инициативе игрока (фокус/Use); три промаха
хэша у MOTORHEAD; навграф; вал `Sequence -5`; повторный SIGSEGV в конце
прогона (бэктрейс из трёх кадров бесполезен, нужен gdb или `-rdynamic`).

---

## Очередь: косметика и техдолг исходников

### D. Мусорные символы перед строками UI/диалогов

Перед текстом на экране возникают лишние символы, например `пЮ@Z1` перед
«Сохранение» (скриншот в переписке). Символы разные от строки к строке.
Похоже на префикс, который движок кладёт перед строкой (ключ локализации,
цветовой/форматный код вида `\xNN`), либо на неверно определённую длину
строки при чтении. Смотреть: чтение таблиц локализации, `CStr`-декодирование
в UI-слое, обработка управляющих последовательностей (в консольных строках
уже видели `\xNN`-коды цвета вроде `cf80`).

---

## Сделано (кратко, по датам)

| Дата | Что |
|---|---|
| 2026-07-30 | Крэш в `Cluster_SetMatrixPalette` (placement-new на NULL из исчерпанной VB-арены); дефолт арены на Linux 4 → 32 МБ, `RIDDICK_VBHEAP`, учёт `[VBM]` |
| 2026-07-30 | `RIDDICK_HWSKIN=1` — движок уведён на аппаратный скиннинг; CPU-конвертация вершин упала примерно в 100 раз, палитры ≤48 костей при потолке 64 (важно для ARM) |
| 2026-07-30 | Тайлинг масок света исправлен: wrap-режим берётся из `CTC_TEXTUREFLAGS_CLAMP_U/_V`, как на PS3 |
| 2026-07-31 | `RIDDICK_AUTOSTART=1` — старт сразу в уровень, минуя фронтенд (сбор логов вместо прохода по меню) |
| 2026-07-31 | Доказано, что скелет и скиннинг здоровы; дефект анимации локализован в слоях AG2 (`Docs/Research_BoneAnimation.md`) |
| 2026-08-03 | Симлинки совместимости (73 шт.) удалены из `Source/`, `#include` переведены на реальные пути — `Tools/fix_symlink_includes.sh`, коммит `2b61bb0` |
| 2026-08-03 | Исходники конвертированы в UTF-8 — `Tools/convert_to_utf8.sh`; правило «CP1252 не конвертировать» снято из `AGENTS.md` и `Docs/*` |
| 2026-08-03 | Прогон `pa2_diner` #1: загрузка диалогов признана исправной (гипотеза о подпапках отменена) |
| 2026-08-03 | Прогон `pa2_diner` #2: версия «звук — корень» отменена измерением (парсер `.xsfxc` цел, 177 `Undefined sound` — контент The Darkness/UI, нулевых длин реплики всего 3). Анимграф признан здоровым по штатной трассе движка; дефект локализован ниже — во времени слоя |

Более ранние этапы порта (сборка, платформенный слой, рендер, звук) — в
журнале `CLAUDE.md` §4.

---

## Известные нерешённые дефекты (фон, не блокеры)

* остаток по освещению: маски прожекторов ложатся под неверным углом /
  в неверных координатах — отдельная задача, есть `RndrGL_dll_decomp.c`
  для сверки;
* заливка QNaN в `CXR_Skeleton::EvalAnim` (под `#ifndef M_RTM`) — отключить,
  чтобы совпадало с ретейлом и не плодило ложные следы;
* `bones=110 / nodes=70` — инстанс скелета больше самого скелета;
* видео (WMV9) не декодируется, звук M3/M4 не доделан, сеть — заглушки.
